name: OpenSTL
channels:
  - conda-forge
  - defaults
dependencies:
  - numpy
  - hickle
  - pip
  - python
  - xarray==0.19.0
  - pip:
    - torch 
    - torchvision 
    - torchaudio
    - decord
    - fvcore
    - lpips
    - nni
    - pandas
    - scikit-image<=0.19.3
    - timm<=0.6.11
    - tqdm
prefix: /opt/anaconda3/envs/openstl